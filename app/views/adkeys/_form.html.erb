<%= form_with(model: adkey, local: true) do |form| %>
  <% if adkey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(adkey.errors.count, "error") %> prohibited this adkey from being saved:</h2>

      <ul>
      <% adkey.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :key %>
    <%= form.text_field :key %>
  </div>

  <div class="field">
    <%= form.label :shop_id %>
    <%= form.text_field :shop_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
